# window dimensions
windowWidth=900
windowHeight=600

# render config
glVersionMajor=4
glVersionMinor=1
pointSize=8
#colorBy types: none, direction, velocity, both
colorBy=none

# simulation config
gravConst=1
minCalcDistance=.1

# particle config
particleCount=2000
friction=.1

# mouse config
mouseMass=100
mouseOnly=0

# calculation config
# managerTypes: cpuPair, cpuStepped
particleManager=cpuStepped