# window dimensions
windowWidth=1200
windowHeight=800

# render config
glVersionMajor=4
glVersionMinor=1
pointSize=1
#colorBy types: none, direction, velocity, both
colorBy=direction

# simulation config
useFixedTimeStep=0
timeStep=.1
worldScale=3
gravConst=1
minCalcDistance=.01

# particle config
particleMass=.01
particleCount=100
friction=.5

# mouse config
mouseMass=200
mouseOnly=1

# calculation config
# managerTypes: cpuPair, cpuStepped
particleManager=cpuStepped