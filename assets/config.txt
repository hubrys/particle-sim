# window dimensions
windowWidth=900
windowHeight=600

# render config
glVersionMajor=1
glVersionMinor=0
pointSize=8
#colorBy types: none, direction, velocity, both
colorBy=direction

# simulation config
gravConst=100
minCalcDistance=.1

# particle config
particleCount=1000
friction=.1

# mouse config
mouseMass=100
mouseOnly=0

# calculation config
# managerTypes: cpuPair, cpuStepped
particleManager=cpuStepped